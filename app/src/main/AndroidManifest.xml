<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.arsenals.sos"
    android:sharedUserId="android.uid.system">
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <application
        android:name=".SOSApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">
        <uses-library android:name="cn.arsenals.sos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".services.ActivityHookService"
            android:process=":ActivityHooker"
            android:exported="false">
        </service>
        <receiver android:name=".kastro.CastBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.arsenals.sos.kastro.CAST_START" />
            </intent-filter>
        </receiver>
    </application>
</manifest>